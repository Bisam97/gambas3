# Gambas Form File 3.0

{ Form Form
  MoveScaled(0,0,64,41)
  Resizable = False
  { btnStart Button
    MoveScaled(46,21,17,6)
    Text = ("Start Calculation \nin background")
  }
  { valMaxPrimes ValueBox
    MoveScaled(12,21,30,4)
    Text = ("200000")
  }
  { Label1 Label
    MoveScaled(0,0,64,18)
    Background = Color.LightForeground
    Border = Border.Plain
    Text = ("Task class allows long running calculations to be executed in spawned\nprocess(es) to prevent UI from blocking during long running calculations.\nIn this example prime numbers up to a max number are calculated which \ntakes some time for big numbes. \n\nTherefore a single Task instance takes care of this calculation in \nthe background.")
  }
  { Label2 Label
    MoveScaled(1,21,11,3)
    Text = ("max prime:")
  }
  { Slider1 Slider
    MoveScaled(1,32,62,4)
  }
  { Label3 Label
    MoveScaled(1,36,23,3)
    Text = ("Number of found primes:")
  }
  { valResult ValueBox
    MoveScaled(25,36,17,4)
    Enabled = False
  }
  { Label4 Label
    MoveScaled(1,29,61,3)
    Text = ("Use this slider to show that the UI is responsive during calculation")
  }
  { calcSpinner Spinner
    MoveScaled(24.2857,36,5,4)
  }
}

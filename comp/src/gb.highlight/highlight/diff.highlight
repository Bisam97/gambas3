diff{Datatype}:
  limit
  from /^diff/
index{Keyword}:
  from /^index/
file{Keyword}:
  match /^(\+\+\+|---)/
  file.path{String}:
    match /.*?\n/
position{Function}:
  from /^@@/ to @@
added{Added}:
  from /^\+/ to "\n"
removed{Removed}:
  from /^-/ to "\n"
normal:
  from /./ to "\n"

' Gambas module file

Public Sub Main()

  Dim hMap As New DeleteMap
  Dim I As Integer
  Dim P As Integer
  
  For I = 1 To 20
    P = Rand(0, 9)
    Print "Delete "; P
    hMap.Add(P)
    Print "5 -> "; hMap.FromReal(5);; "->";; hMap.ToReal(hMap.FromReal(5))
    'hMap.Dump()
  Next

End
